import { Navbar, SideNav } from "src/components";

const UserDashboard = () => {
	return (
		<main>
			<Navbar />
			<SideNav />
		</main>
	);
};

export default UserDashboard;
// User dashbaord page.
